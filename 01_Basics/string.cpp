#include<bits/stdc++.h>
using namespace std;

int main(){

        // string str;
        // cin >> str;
        // cout << str;

        // string message;
        // getline(cin,message);

        // cout << message ;


        // operations
        // + , == , .size() 
        // 

        // take input of n lines
        int n;
        cin >> n;
        cin.ignore();

        while(n--){
                string line;
                getline(cin,line);
                cout << line << endl;
        
        }

        return 0;
}